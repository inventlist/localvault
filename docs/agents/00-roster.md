# LocalVault Agent Roster

Use these stable names in thread `awaiting:` fields and handoffs.

| Name | Type | Use when |
| --- | --- | --- |
| `codex` | alias | Codex owes the next implementation or review step |
| `specification` | role | The CLI/MCP contract needs alignment |
| `staff-engineer` | role | Ruby architecture or implementation review is needed |
| `review` | role | A fresh-context review is needed |
| `operator` | human | The operator must decide or unblock |

Canonical convention:
`../../../74-dev-docs/dev/THREADS.md`.
